import MuiAppBar from 'theme/appbar';

const overrides = {
  // Style sheet name ⚛️
  MuiButton: {
    // Name of the rule
    text: {
      // Some CSS
      color: 'white',
    },
  },
  MuiAppBar,
};

export default overrides;
